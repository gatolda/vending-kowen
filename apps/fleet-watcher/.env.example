# Credenciales del fleet-watcher (corre en el VPS).
# Copiá a .env y completá los secretos:  cp .env.example .env
# (el .env real está en .gitignore — nunca se commitea)

# Supabase (mismo proyecto que la Pi; la secret key lee con bypass de RLS)
SUPABASE_URL=https://pazkqjyzkrxxkephmvhd.supabase.co
SUPABASE_SERVICE_KEY=

# Telegram (de @BotFather + getUpdates)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Tuning (opcionales, traen defaults razonables)
OFFLINE_THRESHOLD_S=300
POLL_INTERVAL_S=30
ALERT_COOLDOWN_S=600
