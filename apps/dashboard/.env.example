# Credenciales del sync a Supabase para el dashboard.
#
# Copiá este archivo a .env (el .env real está en .gitignore — nunca se commitea):
#     cp .env.example .env
#
# URL y MACHINE_ID no son secretos. La SECRET KEY se carga con (SIN sudo):
#     bash ~/vending-kowen/scripts/setup_cloud.sh
# (te la pide oculta y la escribe acá de forma segura; usa sudo solo para reiniciar el servicio)

SUPABASE_URL=https://pazkqjyzkrxxkephmvhd.supabase.co
MACHINE_ID=kowen-01
SUPABASE_SERVICE_KEY=
